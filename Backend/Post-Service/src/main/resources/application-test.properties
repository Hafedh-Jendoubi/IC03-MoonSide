# Test profile – connects to MongoDB service running in CI
spring.data.mongodb.uri=mongodb://${MONGO_ROOT_USERNAME:admin}:${MONGO_ROOT_PASSWORD:admin123}@${MONGODB_HOST:localhost}:${MONGO_PORT:27017}/post_db_test?authSource=admin
spring.data.mongodb.auto-index-creation=true

# Disable Eureka registration during tests
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# JWT (test secret)
jwt.secret=3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# MinIO – dummy values so @Value injection succeeds; the bean itself is mocked
minio.endpoint=http://localhost:9000
minio.access-key=test
minio.secret-key=test
minio.bucket=test-bucket
